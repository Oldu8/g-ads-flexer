This is a monorepo. READ the root `CLAUDE.md` first for the overall shape,
then the `CLAUDE.md` inside whichever of `mcp-server/` or `web/` you're
actually working in for its detailed rules/current task.
